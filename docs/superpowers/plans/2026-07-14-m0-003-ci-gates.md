# M0-003 分层 CI 门禁 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 ItemAll 建立一个单 Workflow、三个独立 Job 的正式 CI 门禁，在 Pull Request 与 `main` push 上验证 Go、Frontend、生成文件、依赖政策、Secret、安全边界和 Windows Wails 构建，并以受控 RED→GREEN 演练证明门禁能够阻断错误。

**Architecture:** `.github/workflows/ci.yml` 只负责环境安装与命令编排；可维护的结构化规则集中在 `scripts/ci` 的 Go 检查器中，并通过普通 Go 单元测试验证。`quality` 与 `generated-and-security` 在 `ubuntu-24.04` 并行运行，`windows-build` 在 `windows-2025` 提供真实目标平台构建与启动事实；三个 Job 名称固定，后续作为 `main` 的 required status checks。

**Tech Stack:** GitHub Actions、Go 1.26.5、Wails v2.13.0、Node.js 24.18.0、pnpm 11.12.0、Gitleaks v8.30.0、PowerShell 7、Ubuntu 24.04 GTK3/WebKitGTK 4.1、Windows Server 2025。

## Global Constraints

- 关联 Issue 固定为 [Issue #7](https://github.com/vvitem/item_all/issues/7)。
- 设计依据固定为 `docs/superpowers/specs/2026-07-13-m0-003-ci-gates-design.md` 与 `DEC-005`。
- Workflow 文件固定为 `.github/workflows/ci.yml`，不得拆成 reusable workflow 或 composite action。
- Job ID 与显示名称固定为 `quality`、`generated-and-security`、`windows-build`。
- Trigger 固定为 `pull_request(main)`、`push(main)` 和 `workflow_dispatch`；不得配置 `paths` 或 `paths-ignore`。
- 顶层权限固定为 `contents: read`；不使用仓库 Secret、环境 Secret、云凭据或写权限。
- 禁止 `pull_request_target`、自托管 Runner、`continue-on-error: true`、`|| true` 和忽略退出码。
- Runner 固定为 `ubuntu-24.04` 与 `windows-2025`，不得使用 `*-latest`。
- Go 固定为 `1.26.5`，Node 固定为 `24.18.0`，pnpm 固定为 `11.12.0`，Wails 固定为 `v2.13.0`。
- Gitleaks 固定为 `v8.30.0`，通过 `go install github.com/gitleaks/gitleaks/v8@v8.30.0` 安装，不引入 Gitleaks Action 或额外 Token。
- Action 引用固定为完整 Commit SHA：
  - `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0`
  - `actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0`
  - `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0`
  - `actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0`
- Ubuntu 编译、vet、test 和 Binding 生成使用 `webkit2_41` Build Tag，并安装 `build-essential`、`pkg-config`、`libgtk-3-dev`、`libwebkit2gtk-4.1-dev`。
- M0-003 不新增 SSH、SFTP、数据库、SQLite 业务表、Operation Bus、AI、凭据、遥测、Release、安装包、签名或长期 Artifact。
- 所有新规则按 TDD 顺序实施：失败测试 → 验证失败原因 → 最小实现 → 通过测试 → 提交。
- 实现 PR 合并前，`M0-003` 只能标记为 `IN_REVIEW`；合并并完成 required checks 自举后才能标记为 `DONE`。

---

## File Map

### CI Workflow

- Create: `.github/workflows/ci.yml` — 三个稳定 Job、只读权限、并发取消、固定 Action SHA 和固定工具链。

### Go CI Checker

- Create: `scripts/ci/main.go` — `dependencies`、`workflow`、`boundary`、`all` 子命令入口。
- Create: `scripts/ci/policy.go` — 违规数据结构、仓库根目录定位、统一调度与稳定错误输出。
- Create: `scripts/ci/dependencies.go` — Go、Node、pnpm、npm 依赖与 `allowBuilds` 政策。
- Create: `scripts/ci/dependencies_test.go` — 正常与非法依赖 Fixture。
- Create: `scripts/ci/workflow.go` — Trigger、权限、Job、Runner、Action SHA、Checkout 深度和危险 Shell 规则。
- Create: `scripts/ci/workflow_test.go` — Workflow 正常与违规 Fixture。
- Create: `scripts/ci/boundary.go` — 禁止文件、远程前端资源、环境变量读取、范围外能力和 Wails Binding Allowlist。
- Create: `scripts/ci/boundary_test.go` — 安全边界正常与违规 Fixture。
- Create: `scripts/ci/smoke-windows.ps1` — Windows EXE 启动观察与完整进程树清理。

### Documentation

- Create: `docs/development/ci.md` — 三个 Job、本地等价命令、故障排查、RED→GREEN 证据和 Branch Protection 自举。
- Modify: `README.md` — 增加 CI 文档入口与本地验证命令。

### Project Tracking

- Modify: `docs/project-management/02-roadmap/backlog.md`
- Modify: `docs/project-management/04-progress/current-status.md`
- Modify: `docs/project-management/04-progress/milestone-status.md`
- Modify: `docs/project-management/04-progress/task-tracking.md`
- Modify: `docs/project-management/04-progress/implementation-trace.md`
- Modify: `docs/project-management/04-progress/weekly-log.md`
- Modify: `docs/project-management/04-progress/changelog.md`

---

## Execution Setup

在实施代码前，使用 `superpowers:using-git-worktrees` 从最新 `origin/main` 创建隔离工作区：

```bash
git fetch origin main
git worktree add .worktrees/m0-003-ci-gates -b feat/m0-003-ci-gates origin/main
cd .worktrees/m0-003-ci-gates
```

基线必须已经包含设计 Merge Commit `3cc4039fcf88882f54be55029541b2f9b638c1d2` 和本 Implementation Plan。若 `origin/main` 不包含二者，停止实施并先同步主分支。

---

### Task 1: 建立可测试的 CI 检查器框架与依赖政策

**Files:**
- Create: `scripts/ci/main.go`
- Create: `scripts/ci/policy.go`
- Create: `scripts/ci/dependencies.go`
- Create: `scripts/ci/dependencies_test.go`

**Interfaces:**
- Consumes: 仓库根目录中的 `go.mod`、`.nvmrc`、`frontend/package.json`、`frontend/pnpm-workspace.yaml`。
- Produces: `runPolicy(name, root string) error`；CLI 命令 `go run ./scripts/ci dependencies` 与 `go run ./scripts/ci all`。

- [ ] **Step 1: 写依赖政策失败测试**

Create `scripts/ci/dependencies_test.go`:

```go
package main

import (
    "os"
    "path/filepath"
    "strings"
    "testing"
)

func TestDependencyPolicyAcceptsRepositoryBaseline(t *testing.T) {
    root := dependencyFixture(t)

    if violations := checkDependencies(root); len(violations) != 0 {
        t.Fatalf("expected valid fixture, got %v", violations)
    }
}

func TestDependencyPolicyRejectsFloatingNPMVersion(t *testing.T) {
    root := dependencyFixture(t)
    writeFixture(t, filepath.Join(root, "frontend", "package.json"), `{
  "packageManager": "pnpm@11.12.0",
  "engines": {"node": "24.18.0", "pnpm": "11.12.0"},
  "dependencies": {"react": "^19.1.0"},
  "devDependencies": {}
}`)

    violations := checkDependencies(root)
    assertViolation(t, violations, "npm-exact-version")
}

func TestDependencyPolicyRejectsAdditionalBuildScriptAuthorization(t *testing.T) {
    root := dependencyFixture(t)
    writeFixture(t, filepath.Join(root, "frontend", "pnpm-workspace.yaml"), `packages:
  - .

allowBuilds:
  esbuild: true
  unknown-native-package: true
`)

    violations := checkDependencies(root)
    assertViolation(t, violations, "pnpm-allow-builds")
}

func TestDependencyPolicyRejectsToolchainDrift(t *testing.T) {
    root := dependencyFixture(t)
    writeFixture(t, filepath.Join(root, "go.mod"), `module github.com/vvitem/item_all

go 1.26.0

toolchain go1.26.4

require github.com/wailsapp/wails/v2 v2.13.0
`)

    violations := checkDependencies(root)
    assertViolation(t, violations, "go-toolchain")
}

func dependencyFixture(t *testing.T) string {
    t.Helper()
    root := t.TempDir()
    writeFixture(t, filepath.Join(root, "go.mod"), `module github.com/vvitem/item_all

go 1.26.0

toolchain go1.26.5

require github.com/wailsapp/wails/v2 v2.13.0
`)
    writeFixture(t, filepath.Join(root, ".nvmrc"), "24.18.0\n")
    writeFixture(t, filepath.Join(root, "frontend", "package.json"), `{
  "packageManager": "pnpm@11.12.0",
  "engines": {"node": "24.18.0", "pnpm": "11.12.0"},
  "dependencies": {"react": "19.1.0"},
  "devDependencies": {"vite": "7.0.0"}
}`)
    writeFixture(t, filepath.Join(root, "frontend", "pnpm-workspace.yaml"), `packages:
  - .

allowBuilds:
  esbuild: true
`)
    return root
}

func writeFixture(t *testing.T, path, content string) {
    t.Helper()
    if err := os.MkdirAll(filepath.Dir(path), 0o755); err != nil {
        t.Fatal(err)
    }
    if err := os.WriteFile(path, []byte(content), 0o600); err != nil {
        t.Fatal(err)
    }
}

func assertViolation(t *testing.T, violations Violations, rule string) {
    t.Helper()
    for _, violation := range violations {
        if violation.Rule == rule {
            return
        }
    }
    t.Fatalf("expected rule %q, got %s", rule, strings.TrimSpace(violations.Error()))
}
```

- [ ] **Step 2: 运行测试并确认 RED**

Run:

```bash
go test ./scripts/ci -run DependencyPolicy -v
```

Expected: FAIL，编译错误明确指出 `checkDependencies`、`Violations` 等尚未定义。

- [ ] **Step 3: 实现统一违规模型与命令入口**

Create `scripts/ci/policy.go`:

```go
package main

import (
    "errors"
    "fmt"
    "os"
    "path/filepath"
    "sort"
    "strings"
)

type Violation struct {
    Path    string
    Rule    string
    Message string
}

type Violations []Violation

func (v *Violations) Add(path, rule, message string) {
    *v = append(*v, Violation{Path: filepath.ToSlash(path), Rule: rule, Message: message})
}

func (v Violations) Error() string {
    if len(v) == 0 {
        return ""
    }
    sort.Slice(v, func(i, j int) bool {
        if v[i].Path != v[j].Path {
            return v[i].Path < v[j].Path
        }
        return v[i].Rule < v[j].Rule
    })
    var builder strings.Builder
    for _, item := range v {
        fmt.Fprintf(&builder, "%s [%s] %s\n", item.Path, item.Rule, item.Message)
    }
    return builder.String()
}

func runPolicy(name, root string) error {
    var violations Violations
    switch name {
    case "dependencies":
        violations = append(violations, checkDependencies(root)...)
    case "workflow":
        violations = append(violations, checkWorkflow(root)...)
    case "boundary":
        violations = append(violations, checkBoundary(root)...)
    case "all":
        violations = append(violations, checkDependencies(root)...)
        violations = append(violations, checkWorkflow(root)...)
        violations = append(violations, checkBoundary(root)...)
    default:
        return fmt.Errorf("unknown policy %q; use dependencies, workflow, boundary, or all", name)
    }
    if len(violations) != 0 {
        return violations
    }
    return nil
}

func findRepositoryRoot(start string) (string, error) {
    current, err := filepath.Abs(start)
    if err != nil {
        return "", err
    }
    for {
        if fileExists(filepath.Join(current, "go.mod")) && fileExists(filepath.Join(current, "frontend", "package.json")) {
            return current, nil
        }
        parent := filepath.Dir(current)
        if parent == current {
            return "", errors.New("repository root not found")
        }
        current = parent
    }
}

func fileExists(path string) bool {
    info, err := os.Stat(path)
    return err == nil && !info.IsDir()
}

func readText(path string) (string, error) {
    data, err := os.ReadFile(path)
    if err != nil {
        return "", err
    }
    return string(data), nil
}
```

Create `scripts/ci/main.go`:

```go
package main

import (
    "fmt"
    "os"
)

func main() {
    if len(os.Args) != 2 {
        fmt.Fprintln(os.Stderr, "usage: go run ./scripts/ci dependencies|workflow|boundary|all")
        os.Exit(2)
    }
    root, err := findRepositoryRoot(".")
    if err != nil {
        fmt.Fprintln(os.Stderr, err)
        os.Exit(1)
    }
    if err := runPolicy(os.Args[1], root); err != nil {
        fmt.Fprint(os.Stderr, err)
        os.Exit(1)
    }
    fmt.Printf("ci policy %s: ok\n", os.Args[1])
}
```

- [ ] **Step 4: 实现依赖政策**

Create `scripts/ci/dependencies.go`:

```go
package main

import (
    "encoding/json"
    "fmt"
    "path/filepath"
    "regexp"
    "sort"
    "strings"
)

const (
    expectedModule       = "github.com/vvitem/item_all"
    expectedGoDirective  = "1.26.0"
    expectedGoToolchain  = "go1.26.5"
    expectedWailsVersion = "v2.13.0"
    expectedNodeVersion  = "24.18.0"
    expectedPNPMVersion  = "11.12.0"
)

var exactVersion = regexp.MustCompile(`^[0-9]+\.[0-9]+\.[0-9]+(?:[-+][0-9A-Za-z.-]+)?$`)

type packageManifest struct {
    PackageManager  string            `json:"packageManager"`
    Engines         map[string]string `json:"engines"`
    Dependencies    map[string]string `json:"dependencies"`
    DevDependencies map[string]string `json:"devDependencies"`
}

func checkDependencies(root string) Violations {
    var violations Violations
    checkGoModule(root, &violations)
    checkNodeManifest(root, &violations)
    checkPNPMWorkspace(root, &violations)
    return violations
}

func checkGoModule(root string, violations *Violations) {
    path := filepath.Join(root, "go.mod")
    text, err := readText(path)
    if err != nil {
        violations.Add("go.mod", "go-mod-readable", err.Error())
        return
    }
    required := map[string]string{
        "go-module":       "module " + expectedModule,
        "go-version":      "go " + expectedGoDirective,
        "go-toolchain":    "toolchain " + expectedGoToolchain,
        "wails-version":   "github.com/wailsapp/wails/v2 " + expectedWailsVersion,
    }
    for rule, token := range required {
        if !strings.Contains(text, token) {
            violations.Add("go.mod", rule, fmt.Sprintf("expected %q", token))
        }
    }
}

func checkNodeManifest(root string, violations *Violations) {
    path := filepath.Join(root, "frontend", "package.json")
    text, err := readText(path)
    if err != nil {
        violations.Add("frontend/package.json", "package-readable", err.Error())
        return
    }
    var manifest packageManifest
    if err := json.Unmarshal([]byte(text), &manifest); err != nil {
        violations.Add("frontend/package.json", "package-json", err.Error())
        return
    }
    if manifest.PackageManager != "pnpm@"+expectedPNPMVersion {
        violations.Add("frontend/package.json", "pnpm-version", "packageManager must be pnpm@"+expectedPNPMVersion)
    }
    if manifest.Engines["node"] != expectedNodeVersion {
        violations.Add("frontend/package.json", "node-version", "engines.node must match .nvmrc")
    }
    if manifest.Engines["pnpm"] != expectedPNPMVersion {
        violations.Add("frontend/package.json", "pnpm-engine", "engines.pnpm must be exact")
    }
    nvmrc, err := readText(filepath.Join(root, ".nvmrc"))
    if err != nil || strings.TrimSpace(nvmrc) != expectedNodeVersion {
        violations.Add(".nvmrc", "node-version", "expected "+expectedNodeVersion)
    }
    names := make([]string, 0, len(manifest.Dependencies)+len(manifest.DevDependencies))
    for name := range manifest.Dependencies {
        names = append(names, name)
    }
    for name := range manifest.DevDependencies {
        names = append(names, name)
    }
    sort.Strings(names)
    for _, name := range names {
        version := manifest.Dependencies[name]
        if version == "" {
            version = manifest.DevDependencies[name]
        }
        if !exactVersion.MatchString(version) {
            violations.Add("frontend/package.json", "npm-exact-version", fmt.Sprintf("%s uses non-exact version %q", name, version))
        }
    }
}

func checkPNPMWorkspace(root string, violations *Violations) {
    path := filepath.Join(root, "frontend", "pnpm-workspace.yaml")
    text, err := readText(path)
    if err != nil {
        violations.Add("frontend/pnpm-workspace.yaml", "pnpm-workspace-readable", err.Error())
        return
    }
    if !strings.Contains(text, "packages:\n  - .") {
        violations.Add("frontend/pnpm-workspace.yaml", "pnpm-packages", "workspace must contain only the frontend package root")
    }
    lines := strings.Split(text, "\n")
    inAllowBuilds := false
    allowed := map[string]bool{}
    for _, line := range lines {
        if strings.TrimSpace(line) == "allowBuilds:" {
            inAllowBuilds = true
            continue
        }
        if !inAllowBuilds {
            continue
        }
        if strings.TrimSpace(line) == "" {
            continue
        }
        if !strings.HasPrefix(line, "  ") {
            inAllowBuilds = false
            continue
        }
        parts := strings.SplitN(strings.TrimSpace(line), ":", 2)
        if len(parts) == 2 && strings.TrimSpace(parts[1]) == "true" {
            allowed[parts[0]] = true
        }
    }
    if len(allowed) != 1 || !allowed["esbuild"] {
        violations.Add("frontend/pnpm-workspace.yaml", "pnpm-allow-builds", "allowBuilds must contain only esbuild: true")
    }
}
```

- [ ] **Step 5: 运行依赖政策测试并确认 GREEN**

Run:

```bash
gofmt -w scripts/ci/*.go
go test ./scripts/ci -run DependencyPolicy -v
go run ./scripts/ci dependencies
```

Expected: 全部 PASS，CLI 输出 `ci policy dependencies: ok`。

- [ ] **Step 6: 提交 Task 1**

```bash
git add scripts/ci/main.go scripts/ci/policy.go scripts/ci/dependencies.go scripts/ci/dependencies_test.go
git commit -m "test: add CI dependency policy checker"
```

---

### Task 2: 实现 Workflow 静态与供应链检查

**Files:**
- Create: `scripts/ci/workflow.go`
- Create: `scripts/ci/workflow_test.go`

**Interfaces:**
- Consumes: `.github/workflows/ci.yml` 文本。
- Produces: `checkWorkflow(root string) Violations`，验证稳定 Job、权限、Trigger、Runner、Checkout 深度、Action SHA 与危险 Shell 模式。

- [ ] **Step 1: 写 Workflow 规则失败测试**

Create `scripts/ci/workflow_test.go`:

```go
package main

import (
    "path/filepath"
    "strings"
    "testing"
)

func TestWorkflowPolicyAcceptsLayeredBaseline(t *testing.T) {
    root := dependencyFixture(t)
    writeFixture(t, filepath.Join(root, ".github", "workflows", "ci.yml"), validWorkflowFixture)

    if violations := checkWorkflow(root); len(violations) != 0 {
        t.Fatalf("expected valid workflow, got %s", violations.Error())
    }
}

func TestWorkflowPolicyRejectsMutableActionTag(t *testing.T) {
    root := dependencyFixture(t)
    invalid := strings.Replace(validWorkflowFixture,
        "actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0",
        "actions/checkout@v7",
        1,
    )
    writeFixture(t, filepath.Join(root, ".github", "workflows", "ci.yml"), invalid)

    assertViolation(t, checkWorkflow(root), "action-sha")
}

func TestWorkflowPolicyRejectsWritePermission(t *testing.T) {
    root := dependencyFixture(t)
    invalid := strings.Replace(validWorkflowFixture, "contents: read", "contents: write", 1)
    writeFixture(t, filepath.Join(root, ".github", "workflows", "ci.yml"), invalid)

    assertViolation(t, checkWorkflow(root), "permissions")
}

func TestWorkflowPolicyRejectsShallowSecurityCheckout(t *testing.T) {
    root := dependencyFixture(t)
    invalid := strings.Replace(validWorkflowFixture, "fetch-depth: 0", "fetch-depth: 1", 1)
    writeFixture(t, filepath.Join(root, ".github", "workflows", "ci.yml"), invalid)

    assertViolation(t, checkWorkflow(root), "security-history")
}

func TestWorkflowPolicyRejectsPullRequestTarget(t *testing.T) {
    root := dependencyFixture(t)
    invalid := strings.Replace(validWorkflowFixture, "pull_request:", "pull_request_target:", 1)
    writeFixture(t, filepath.Join(root, ".github", "workflows", "ci.yml"), invalid)

    assertViolation(t, checkWorkflow(root), "pull-request-target")
}

const validWorkflowFixture = `name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  quality:
    name: quality
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
  generated-and-security:
    name: generated-and-security
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
  windows-build:
    name: windows-build
    runs-on: windows-2025
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          persist-credentials: false
      - uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
`
```

- [ ] **Step 2: 运行测试并确认 RED**

```bash
go test ./scripts/ci -run WorkflowPolicy -v
```

Expected: FAIL，编译错误明确指出 `checkWorkflow` 尚未定义。

- [ ] **Step 3: 实现 Workflow 检查器**

Create `scripts/ci/workflow.go`:

```go
package main

import (
    "fmt"
    "path/filepath"
    "regexp"
    "strings"
)

var pinnedAction = regexp.MustCompile(`^\s*(?:-\s+)?uses:\s+([A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+)@([0-9a-f]{40})\s+#\s+v[0-9][^\s]*\s*$`)
var writePermission = regexp.MustCompile(`(?m)^\s+[A-Za-z0-9_-]+:\s+write\s*$`)

var requiredActions = map[string]string{
    "actions/checkout":   "9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0",
    "actions/setup-go":   "924ae3a1cded613372ab5595356fb5720e22ba16",
    "actions/setup-node": "820762786026740c76f36085b0efc47a31fe5020",
    "actions/cache":      "55cc8345863c7cc4c66a329aec7e433d2d1c52a9",
}

func checkWorkflow(root string) Violations {
    var violations Violations
    path := filepath.Join(root, ".github", "workflows", "ci.yml")
    text, err := readText(path)
    if err != nil {
        violations.Add(".github/workflows/ci.yml", "workflow-readable", err.Error())
        return violations
    }
    mustContain := map[string]string{
        "trigger-pr":            "  pull_request:",
        "trigger-push":          "  push:",
        "trigger-manual":        "  workflow_dispatch:",
        "permissions":           "  contents: read",
        "concurrency-group":     "group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}",
        "concurrency-cancel":    "cancel-in-progress: true",
    }
    for rule, token := range mustContain {
        if !strings.Contains(text, token) {
            violations.Add(".github/workflows/ci.yml", rule, fmt.Sprintf("expected %q", token))
        }
    }
    forbidden := map[string]string{
        "pull-request-target": "pull_request_target",
        "path-filter":         "paths:",
        "path-ignore":         "paths-ignore:",
        "continue-on-error":   "continue-on-error:",
        "ignored-exit-code":   "|| true",
        "write-all":           "write-all",
    }
    for rule, token := range forbidden {
        if strings.Contains(text, token) {
            violations.Add(".github/workflows/ci.yml", rule, fmt.Sprintf("forbidden token %q", token))
        }
    }
    if writePermission.MatchString(text) {
        violations.Add(".github/workflows/ci.yml", "permissions", "workflow and jobs must not request any write permission")
    }

    expectedJobs := map[string]struct {
        runner  string
        timeout string
        depth   string
    }{
        "quality":                {runner: "ubuntu-24.04", timeout: "15", depth: "1"},
        "generated-and-security": {runner: "ubuntu-24.04", timeout: "15", depth: "0"},
        "windows-build":          {runner: "windows-2025", timeout: "25", depth: "1"},
    }
    for job, expectation := range expectedJobs {
        block := workflowJobBlock(text, job)
        if block == "" {
            violations.Add(".github/workflows/ci.yml", "job-"+job, "required job is missing")
            continue
        }
        required := []string{
            "name: " + job,
            "runs-on: " + expectation.runner,
            "timeout-minutes: " + expectation.timeout,
            "fetch-depth: " + expectation.depth,
            "persist-credentials: false",
        }
        for _, token := range required {
            if !strings.Contains(block, token) {
                rule := "job-" + job
                if job == "generated-and-security" && strings.HasPrefix(token, "fetch-depth:") {
                    rule = "security-history"
                }
                violations.Add(".github/workflows/ci.yml", rule, fmt.Sprintf("expected %q", token))
            }
        }
    }

    for _, line := range strings.Split(text, "\n") {
        if !strings.Contains(line, "uses:") {
            continue
        }
        match := pinnedAction.FindStringSubmatch(line)
        if match == nil {
            violations.Add(".github/workflows/ci.yml", "action-sha", "every uses entry must use a 40-character SHA and version comment")
            continue
        }
        if expected, ok := requiredActions[match[1]]; ok && match[2] != expected {
            violations.Add(".github/workflows/ci.yml", "action-version", fmt.Sprintf("%s must use %s", match[1], expected))
        }
    }
    checkUntrustedRunContext(text, &violations)
    return violations
}

func workflowJobBlock(text, job string) string {
    lines := strings.Split(text, "\n")
    marker := "  " + job + ":"
    start := -1
    for index, line := range lines {
        if line == marker {
            start = index
            break
        }
    }
    if start == -1 {
        return ""
    }
    end := len(lines)
    for index := start + 1; index < len(lines); index++ {
        line := lines[index]
        if strings.HasPrefix(line, "  ") && !strings.HasPrefix(line, "    ") && strings.HasSuffix(line, ":") {
            end = index
            break
        }
    }
    return strings.Join(lines[start:end], "\n")
}

func checkUntrustedRunContext(text string, violations *Violations) {
    risky := []string{
        "github.event.pull_request.title",
        "github.event.pull_request.body",
        "github.event.pull_request.head.label",
        "github.head_ref",
        "github.event.issue",
    }
    lines := strings.Split(text, "\n")
    inRun := false
    runIndent := 0
    for _, line := range lines {
        trimmed := strings.TrimSpace(line)
        indent := len(line) - len(strings.TrimLeft(line, " "))
        if strings.HasPrefix(trimmed, "run:") {
            inRun = true
            runIndent = indent
        } else if inRun && trimmed != "" && indent <= runIndent {
            inRun = false
        }
        if !inRun {
            continue
        }
        for _, token := range risky {
            if strings.Contains(line, token) {
                violations.Add(".github/workflows/ci.yml", "untrusted-context", fmt.Sprintf("run block contains %s", token))
            }
        }
    }
}
```

- [ ] **Step 4: 运行 Workflow 测试并确认 GREEN**

```bash
gofmt -w scripts/ci/workflow.go scripts/ci/workflow_test.go
go test ./scripts/ci -run WorkflowPolicy -v
```

Expected: 全部 PASS。

- [ ] **Step 5: 提交 Task 2**

```bash
git add scripts/ci/workflow.go scripts/ci/workflow_test.go
git commit -m "test: enforce GitHub Actions workflow policy"
```

---

### Task 3: 实现仓库安全边界与 Windows 烟测脚本

**Files:**
- Create: `scripts/ci/boundary.go`
- Create: `scripts/ci/boundary_test.go`
- Create: `scripts/ci/smoke-windows.ps1`

**Interfaces:**
- Consumes: 当前仓库文件树和 `frontend/wailsjs/go/main/App.js`。
- Produces: `checkBoundary(root string) Violations`；PowerShell 命令 `scripts/ci/smoke-windows.ps1 -Executable build/bin/ItemAll.exe -ObservationSeconds 10`。

- [ ] **Step 1: 写安全边界失败测试**

Create `scripts/ci/boundary_test.go`:

```go
package main

import (
    "path/filepath"
    "testing"
)

func TestBoundaryPolicyAcceptsMinimalApplication(t *testing.T) {
    root := boundaryFixture(t)

    if violations := checkBoundary(root); len(violations) != 0 {
        t.Fatalf("expected valid boundary, got %s", violations.Error())
    }
}

func TestBoundaryPolicyRejectsPrivateKeyFile(t *testing.T) {
    root := boundaryFixture(t)
    writeFixture(t, filepath.Join(root, "test.key"), "not-a-real-key")

    assertViolation(t, checkBoundary(root), "secret-file")
}

func TestBoundaryPolicyRejectsRemoteFrontendResource(t *testing.T) {
    root := boundaryFixture(t)
    writeFixture(t, filepath.Join(root, "frontend", "src", "styles.css"), `@import url("https://example.invalid/font.css");`)

    assertViolation(t, checkBoundary(root), "remote-resource")
}

func TestBoundaryPolicyRejectsEnvironmentRead(t *testing.T) {
    root := boundaryFixture(t)
    writeFixture(t, filepath.Join(root, "app.go"), "package main\nimport \"os\"\nvar value = os.Getenv(\"TOKEN\")\n")

    assertViolation(t, checkBoundary(root), "environment-read")
}

func TestBoundaryPolicyRejectsAdditionalWailsBinding(t *testing.T) {
    root := boundaryFixture(t)
    writeFixture(t, filepath.Join(root, "frontend", "wailsjs", "go", "main", "App.js"), `export function GetAppInfo() {}
export function ReadFile() {}
`)

    assertViolation(t, checkBoundary(root), "wails-binding")
}

func boundaryFixture(t *testing.T) string {
    t.Helper()
    root := dependencyFixture(t)
    writeFixture(t, filepath.Join(root, "app.go"), "package main\n")
    writeFixture(t, filepath.Join(root, "frontend", "src", "App.tsx"), "export default function App() { return null }\n")
    writeFixture(t, filepath.Join(root, "frontend", "wailsjs", "go", "main", "App.js"), "export function GetAppInfo() {}\n")
    return root
}
```

- [ ] **Step 2: 运行测试并确认 RED**

```bash
go test ./scripts/ci -run BoundaryPolicy -v
```

Expected: FAIL，编译错误明确指出 `checkBoundary` 尚未定义。

- [ ] **Step 3: 实现安全边界检查器**

Create `scripts/ci/boundary.go`:

```go
package main

import (
    "io/fs"
    "path/filepath"
    "regexp"
    "sort"
    "strings"
)

var remoteResource = regexp.MustCompile(`(?i)(https?:)?//[^\s"')]+`)
var bindingExport = regexp.MustCompile(`(?m)^export function ([A-Za-z0-9_]+)\(`)

func checkBoundary(root string) Violations {
    var violations Violations
    forbiddenPaths := []string{
        "internal/ssh",
        "internal/sftp",
        "internal/database",
        "internal/operation",
        "internal/ai",
        "internal/credential",
        "internal/telemetry",
    }
    forbiddenNames := map[string]bool{
        ".env": true,
        ".env.local": true,
        "id_rsa": true,
        "id_ed25519": true,
    }
    forbiddenExtensions := map[string]bool{
        ".pem": true,
        ".key": true,
        ".p12": true,
        ".pfx": true,
    }

    _ = filepath.WalkDir(root, func(path string, entry fs.DirEntry, walkErr error) error {
        if walkErr != nil {
            violations.Add(relative(root, path), "walk", walkErr.Error())
            return nil
        }
        rel := relative(root, path)
        if entry.IsDir() {
            if rel == ".git" || rel == "frontend/node_modules" || rel == "frontend/dist" || rel == "build/bin" {
                return filepath.SkipDir
            }
            for _, forbidden := range forbiddenPaths {
                if rel == forbidden || strings.HasPrefix(rel, forbidden+"/") {
                    violations.Add(rel, "scope-directory", "capability is outside M0-003")
                }
            }
            return nil
        }
        lowerName := strings.ToLower(entry.Name())
        if forbiddenNames[lowerName] || forbiddenExtensions[strings.ToLower(filepath.Ext(lowerName))] {
            violations.Add(rel, "secret-file", "secret-bearing file type is forbidden")
        }
        if shouldScanFrontend(rel) || shouldScanSource(rel) {
            text, err := readText(path)
            if err != nil {
                violations.Add(rel, "source-readable", err.Error())
                return nil
            }
            if shouldScanFrontend(rel) && remoteResource.MatchString(text) {
                violations.Add(rel, "remote-resource", "frontend source must not load remote scripts, styles, fonts, or CDN resources")
            }
            if shouldScanSource(rel) {
                envTokens := []string{"os.Getenv(", "os.LookupEnv(", "process.env", "import.meta.env"}
                for _, token := range envTokens {
                    if strings.Contains(text, token) {
                        violations.Add(rel, "environment-read", "application source must not read environment variables in M0-003")
                    }
                }
                capabilityTokens := []string{
                    "golang.org/x/crypto/ssh",
                    "database/sql",
                    "github.com/openai/",
                    "go.opentelemetry.io/",
                }
                for _, token := range capabilityTokens {
                    if strings.Contains(text, token) {
                        violations.Add(rel, "scope-import", "capability dependency is outside M0-003: "+token)
                    }
                }
            }
        }
        return nil
    })
    checkBindings(root, &violations)
    return violations
}

func checkBindings(root string, violations *Violations) {
    path := filepath.Join(root, "frontend", "wailsjs", "go", "main", "App.js")
    text, err := readText(path)
    if err != nil {
        violations.Add("frontend/wailsjs/go/main/App.js", "wails-binding", err.Error())
        return
    }
    matches := bindingExport.FindAllStringSubmatch(text, -1)
    names := make([]string, 0, len(matches))
    for _, match := range matches {
        names = append(names, match[1])
    }
    sort.Strings(names)
    if len(names) != 1 || names[0] != "GetAppInfo" {
        violations.Add("frontend/wailsjs/go/main/App.js", "wails-binding", "only GetAppInfo may be exported")
    }
}

func relative(root, path string) string {
    rel, err := filepath.Rel(root, path)
    if err != nil {
        return filepath.ToSlash(path)
    }
    return filepath.ToSlash(rel)
}

func shouldScanFrontend(rel string) bool {
    return rel == "frontend/index.html" || strings.HasPrefix(rel, "frontend/src/")
}

func shouldScanSource(rel string) bool {
    if strings.HasPrefix(rel, "scripts/ci/") || strings.HasPrefix(rel, "frontend/wailsjs/") {
        return false
    }
    extension := strings.ToLower(filepath.Ext(rel))
    return extension == ".go" || extension == ".ts" || extension == ".tsx" || extension == ".js" || extension == ".jsx"
}
```

- [ ] **Step 4: 运行安全边界测试并确认 GREEN**

```bash
gofmt -w scripts/ci/boundary.go scripts/ci/boundary_test.go
go test ./scripts/ci -run BoundaryPolicy -v
go run ./scripts/ci boundary
```

Expected: 全部 PASS，CLI 输出 `ci policy boundary: ok`。

- [ ] **Step 5: 创建 Windows 进程烟测脚本**

Create `scripts/ci/smoke-windows.ps1`:

```powershell
[CmdletBinding()]
param(
    [Parameter(Mandatory = $true)]
    [string]$Executable,

    [ValidateRange(1, 60)]
    [int]$ObservationSeconds = 10
)

$ErrorActionPreference = 'Stop'
$resolvedExecutable = (Resolve-Path -LiteralPath $Executable).Path
$process = Start-Process -FilePath $resolvedExecutable -PassThru

try {
    Start-Sleep -Seconds $ObservationSeconds
    $process.Refresh()
    if ($process.HasExited) {
        throw "ItemAll exited during the $ObservationSeconds second observation window with code $($process.ExitCode)."
    }
    Write-Host "ItemAll remained running for $ObservationSeconds seconds."
}
finally {
    $process.Refresh()
    if (-not $process.HasExited) {
        $cleanup = Start-Process -FilePath taskkill.exe -ArgumentList @('/PID', $process.Id, '/T', '/F') -Wait -PassThru -NoNewWindow
        if ($cleanup.ExitCode -ne 0) {
            throw "Failed to clean the ItemAll process tree; taskkill exit code $($cleanup.ExitCode)."
        }
    }
}
```

- [ ] **Step 6: 执行全量 Go 检查器测试**

```bash
gofmt -w scripts/ci/*.go
go test ./scripts/ci -v
go run ./scripts/ci dependencies
```

Expected: 全部 PASS。此时 `workflow` 和 `all` 子命令仍因正式 Workflow 尚不存在而失败，这是 Task 4 的预期前置状态。

- [ ] **Step 7: 提交 Task 3**

```bash
git add scripts/ci/boundary.go scripts/ci/boundary_test.go scripts/ci/smoke-windows.ps1
git commit -m "test: enforce M0-003 repository security boundary"
```

---

### Task 4: 创建三 Job 正式 Workflow 并完成自举 GREEN

**Files:**
- Create: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: Task 1–3 的 Go 检查器与 Windows 烟测脚本。
- Produces: 三个稳定状态检查 `quality`、`generated-and-security`、`windows-build`。

- [ ] **Step 1: 证明 Workflow 文件缺失会失败**

Run:

```bash
go run ./scripts/ci workflow
```

Expected: FAIL，规则 `workflow-readable` 指出 `.github/workflows/ci.yml` 不存在。

- [ ] **Step 2: 创建正式 Workflow**

Create `.github/workflows/ci.yml`:

```yaml
name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: '1.26.5'
  NODE_VERSION: '24.18.0'
  PNPM_VERSION: '11.12.0'
  WAILS_VERSION: 'v2.13.0'
  GITLEAKS_VERSION: 'v8.30.0'

jobs:
  quality:
    name: quality
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable exact pnpm
        shell: bash
        run: |
          corepack enable
          corepack prepare "pnpm@${PNPM_VERSION}" --activate
          test "$(pnpm --version)" = "${PNPM_VERSION}"

      - name: Resolve pnpm store
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install Ubuntu Wails dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            build-essential \
            pkg-config \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev

      - name: Verify toolchain
        shell: bash
        run: |
          test "$(go env GOVERSION)" = "go${GO_VERSION}"
          test "$(node --version)" = "v${NODE_VERSION}"
          test "$(pnpm --version)" = "${PNPM_VERSION}"

      - name: Test CI policy code
        shell: bash
        run: go test ./scripts/ci -v

      - name: Verify Go module and formatting
        shell: bash
        run: |
          go mod download
          go mod verify
          mapfile -t go_files < <(git ls-files '*.go')
          unformatted="$(gofmt -l "${go_files[@]}")"
          if [[ -n "$unformatted" ]]; then
            printf 'Go files require gofmt:\n%s\nRun: gofmt -w %s\n' "$unformatted" "$unformatted"
            exit 1
          fi

      - name: Run Go vet and tests
        shell: bash
        run: |
          go vet -tags=webkit2_41 ./...
          go test -tags=webkit2_41 ./...
          go test -race ./internal/buildinfo

      - name: Install frontend dependencies
        working-directory: frontend
        run: pnpm install --frozen-lockfile

      - name: Run frontend tests
        working-directory: frontend
        run: pnpm test:run

      - name: Run frontend typecheck
        working-directory: frontend
        run: pnpm typecheck

      - name: Run frontend lint
        working-directory: frontend
        run: pnpm lint

      - name: Build frontend
        working-directory: frontend
        run: pnpm build

  generated-and-security:
    name: generated-and-security
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      GOFLAGS: -tags=webkit2_41
    steps:
      - name: Checkout full history
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable exact pnpm
        shell: bash
        run: |
          corepack enable
          corepack prepare "pnpm@${PNPM_VERSION}" --activate
          test "$(pnpm --version)" = "${PNPM_VERSION}"

      - name: Resolve pnpm store
        id: pnpm-store
        shell: bash
        run: echo "path=$(pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - name: Cache pnpm store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install Ubuntu Wails dependencies
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install --yes --no-install-recommends \
            build-essential \
            pkg-config \
            libgtk-3-dev \
            libwebkit2gtk-4.1-dev

      - name: Install exact CI tools
        shell: bash
        run: |
          go install "github.com/wailsapp/wails/v2/cmd/wails@${WAILS_VERSION}"
          go install "github.com/gitleaks/gitleaks/v8@${GITLEAKS_VERSION}"
          wails version
          gitleaks version

      - name: Verify workflow and dependency policy
        shell: bash
        run: |
          go run ./scripts/ci workflow
          go run ./scripts/ci dependencies
          go run ./scripts/ci boundary

      - name: Regenerate Wails bindings
        shell: bash
        run: wails generate module

      - name: Verify module and lockfile drift
        shell: bash
        run: |
          go mod tidy
          pnpm --dir frontend install --frozen-lockfile
          git diff --exit-code -- \
            go.mod \
            go.sum \
            frontend/pnpm-lock.yaml \
            frontend/wailsjs

      - name: Scan full Git history for secrets
        shell: bash
        run: gitleaks git --redact --no-banner .

  windows-build:
    name: windows-build
    runs-on: windows-2025
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6.5.0
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable exact pnpm
        shell: pwsh
        run: |
          corepack enable
          corepack prepare "pnpm@$env:PNPM_VERSION" --activate
          if ((pnpm --version) -ne $env:PNPM_VERSION) {
            throw "pnpm version does not match $env:PNPM_VERSION"
          }

      - name: Resolve pnpm store
        id: pnpm-store
        shell: pwsh
        run: '"path=$(pnpm store path --silent)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append'

      - name: Cache pnpm store
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: ${{ runner.os }}-pnpm-${{ hashFiles('frontend/pnpm-lock.yaml') }}
          restore-keys: ${{ runner.os }}-pnpm-

      - name: Install exact Wails CLI
        shell: pwsh
        run: |
          go install "github.com/wailsapp/wails/v2/cmd/wails@$env:WAILS_VERSION"
          wails version

      - name: Install frontend dependencies
        working-directory: frontend
        run: pnpm install --frozen-lockfile

      - name: Run Go tests
        run: go test ./...

      - name: Build ItemAll
        run: wails build -clean -trimpath

      - name: Verify executable exists
        shell: pwsh
        run: |
          $item = Get-Item -LiteralPath build/bin/ItemAll.exe
          if ($item.Length -le 0) {
            throw 'build/bin/ItemAll.exe is empty.'
          }
          Write-Host "ItemAll.exe size: $($item.Length) bytes"

      - name: Smoke test ItemAll
        shell: pwsh
        run: ./scripts/ci/smoke-windows.ps1 -Executable build/bin/ItemAll.exe -ObservationSeconds 10
```

- [ ] **Step 3: 运行本地 Workflow 和全部规则测试**

```bash
gofmt -w scripts/ci/*.go
go test ./scripts/ci -v
go run ./scripts/ci workflow
go run ./scripts/ci dependencies
go run ./scripts/ci boundary
```

Expected: 全部 PASS。

- [ ] **Step 4: 运行现有应用测试与前端检查**

```bash
go test ./...
cd frontend
pnpm install --frozen-lockfile
pnpm test:run
pnpm typecheck
pnpm lint
pnpm build
```

Expected: 全部 PASS。Linux 本地缺少 Wails 原生依赖时，按 Task 5 文档安装 Ubuntu 24.04 依赖并使用 `-tags=webkit2_41`；不得删除根包测试。

- [ ] **Step 5: 提交 Workflow**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add layered quality security and Windows gates"
```

- [ ] **Step 6: 推送实现分支并确认三个 Job 首次运行**

```bash
cat > /tmp/m0-003-pr.md <<'PRBODY'
## Scope

Implements Issue #7 (`M0-003`) with three stable CI gates: `quality`, `generated-and-security`, and `windows-build`.

## Boundaries

No SSH, database, SQLite business tables, Operation Bus, AI, credentials, telemetry, Release, installer, signing, or long-lived executable artifact.

## Validation

The PR will record the initial green run, one controlled RED run, the restored GREEN run, Windows build/smoke evidence, and the final project-tracking state.
PRBODY

git push -u origin feat/m0-003-ci-gates
gh pr create --draft \
  --base main \
  --head feat/m0-003-ci-gates \
  --title "ci: establish ItemAll quality and Windows gates" \
  --body-file /tmp/m0-003-pr.md
```

Expected: Draft PR 出现 `quality`、`generated-and-security`、`windows-build` 三个独立状态检查。此步骤执行时使用实际实现分支 `feat/m0-003-ci-gates`，计划分支 `docs/m0-003-ci-plan` 不承载 Workflow。

- [ ] **Step 7: 修复首次 Runner 差异直到三个 Job GREEN**

每次失败必须先读取失败 Job 的完整日志，记录根因，再做最小修复。禁止通过放宽权限、删除检查、加入 `continue-on-error` 或改用 `*-latest` 获得绿色。

- [ ] **Step 8: 提交 Task 4 的 Runner 修复**

```bash
git add .github/workflows/ci.yml scripts/ci
git commit -m "ci: make layered gates reproducible on hosted runners"
```

只有存在真实 Runner 修复时创建此提交；没有修复时跳过提交命令，不创建空提交。

---

### Task 5: 编写 CI 开发文档并同步实现评审状态

**Files:**
- Create: `docs/development/ci.md`
- Modify: `README.md`
- Modify: 7 个项目进度文件

**Interfaces:**
- Consumes: 三个 Job 的实际命令、Task 1–4 的本地工具和 Issue #7。
- Produces: 可由新贡献者复现的 CI 文档；`M0-003=IN_REVIEW` 的真实项目账本。

- [ ] **Step 1: 创建 CI 开发文档**

Create `docs/development/ci.md`，正文至少包含以下完整内容：

```markdown
# ItemAll CI 开发与故障排查

ItemAll 使用 `.github/workflows/ci.yml` 的三个独立门禁：

- `quality`：Go、CI 检查器、Frontend 测试与构建。
- `generated-and-security`：Binding/锁文件漂移、依赖政策、仓库边界和完整历史 Secret Scan。
- `windows-build`：Windows Wails production build 与 `ItemAll.exe` 启动烟测。

## 固定工具链

- Go 1.26.5
- Wails v2.13.0
- Node.js 24.18.0
- pnpm 11.12.0
- Gitleaks v8.30.0

## 本地质量检查

```powershell
go test ./scripts/ci -v
go run ./scripts/ci dependencies
go run ./scripts/ci workflow
go run ./scripts/ci boundary
go test ./...
Set-Location frontend
pnpm install --frozen-lockfile
pnpm test:run
pnpm typecheck
pnpm lint
pnpm build
```

Ubuntu 24.04 需要安装 `build-essential`、`pkg-config`、`libgtk-3-dev` 和 `libwebkit2gtk-4.1-dev`，并对 Wails 根包命令使用 `-tags=webkit2_41`。

## 生成文件与锁文件

```powershell
go install github.com/wailsapp/wails/v2/cmd/wails@v2.13.0
wails generate module
go mod tidy
Set-Location frontend
pnpm install --frozen-lockfile
```

重新生成后必须检查 `frontend/wailsjs`、`go.mod`、`go.sum` 和 `frontend/pnpm-lock.yaml`。CI 不自动提交生成结果。

## pnpm allowBuilds

`frontend/pnpm-workspace.yaml` 当前只批准 `esbuild: true`。新增安装脚本授权必须在独立依赖或功能设计中说明原因，并更新测试；禁止使用全局允许策略。

## Secret Scan

```powershell
go install github.com/gitleaks/gitleaks/v8@v8.30.0
gitleaks git --redact --no-banner .
```

CI 使用完整 Git 历史。误报必须采用最小、可解释的局部处理，禁止宽泛排除整个目录或规则；不得把真实 Token 或私钥提交到测试分支。

## Windows 构建与烟测

```powershell
wails build -clean -trimpath
./scripts/ci/smoke-windows.ps1 -Executable build/bin/ItemAll.exe -ObservationSeconds 10
```

烟测只证明 EXE 可加载且没有立即崩溃，不替代人工 UI 或后续 Windows E2E。

## Action SHA 核验

所有 `uses:` 都必须固定到官方仓库 Release/Tag 对应的完整 Commit SHA，并保留版本注释。升级时通过官方 Tag 解析 SHA，独立提交升级 PR，不直接使用大版本 Tag。

## required checks 自举

1. 在 CI 实现 PR 中让三个 Job 运行并变绿。
2. 完成一次受控 RED→GREEN 演练。
3. 合并实现 PR。
4. 在 `main` Branch Protection 中要求 `quality`、`generated-and-security`、`windows-build`。
5. 使用 M0-003 状态同步 PR 验证 required checks 确实阻断未通过的合并。

## 故障原则

失败必须修复根因。禁止 `continue-on-error`、`|| true`、忽略退出码、降低权限扫描、删除检查或把失败仅转换为 warning。
```

- [ ] **Step 2: 更新 README CI 入口**

在 README 的开发或文档入口区域增加：

```markdown
- [CI 门禁与本地复现](docs/development/ci.md)：Go、Frontend、生成文件、安全扫描和 Windows Wails 构建。
```

并增加本地总入口：

```powershell
go test ./scripts/ci -v
go run ./scripts/ci all
```

- [ ] **Step 3: 将 M0-003 同步为 IN_REVIEW**

在 7 个项目进度文件中统一更新：

- `M0-003`: `READY → IN_REVIEW`
- Issue/PR：关联 Issue #7 与实际实现 PR
- 当前总数：`DONE=2`、`IN_REVIEW=1`、`NOT_STARTED=47`
- `M0-foundation`: 已完成 2，进行中/评审 1，READY 0，完成率仍为 `2/10 = 20%`
- 当前正在进行：CI 三 Job 实现与 RED→GREEN 验证
- 测试证据：只记录已经发生的 Run，不预写成功
- `M0-004` 保持 `NOT_STARTED`，直到 M0-003 合并和 required checks 生效

- [ ] **Step 4: 运行文档与规则一致性检查**

```bash
go test ./scripts/ci -v
go run ./scripts/ci all
git diff --check
```

Expected: 全部 PASS；项目状态总数为 50，且没有提前标记 `DONE`。

- [ ] **Step 5: 提交文档与进度状态**

```bash
git add README.md docs/development/ci.md docs/project-management
git commit -m "docs: document CI gates and review status"
```

---

### Task 6: 执行受控 RED→GREEN 证明并完成实现 PR 评审

**Files:**
- Temporary on feature branch: `scripts/ci/red_gate_probe.go`
- Modify: `docs/development/ci.md` — 写入实际 Run 证据
- Modify: 实现 PR 描述

**Interfaces:**
- Consumes: 已运行的正式 Workflow。
- Produces: 一个明确失败 Run、一个恢复绿色 Run、最终无失败 Fixture 的 Ready for Review PR。

- [ ] **Step 1: 确认当前基线三个 Job GREEN**

```bash
gh run list --workflow ci.yml --branch feat/m0-003-ci-gates --limit 3
```

Expected: 最新提交对应的 `quality`、`generated-and-security`、`windows-build` 全部成功。若任何 Job 未成功，先修复，不进入 RED 演练。

- [ ] **Step 2: 创建唯一的 gofmt 受控失败**

Create `scripts/ci/red_gate_probe.go`:

```go
package main

func redGateProbe( ) { }
```

Commit and push:

```bash
git add scripts/ci/red_gate_probe.go
git commit -m "test: prove CI rejects unformatted Go"
git push
```

Expected: `quality` 在 `Verify Go module and formatting` 步骤失败；另外两个 Job 不应因该 Fixture 产生安全或构建范围外失败。

- [ ] **Step 3: 记录 RED Run ID 与失败步骤**

```bash
RED_RUN_ID=$(gh run list --workflow ci.yml --branch feat/m0-003-ci-gates --limit 1 --json databaseId --jq '.[0].databaseId')
gh run view "$RED_RUN_ID" --json databaseId,conclusion,jobs
printf '\n## RED→GREEN Evidence\n\n- RED Run `%s`: `quality` rejected an unformatted Go fixture at `Verify Go module and formatting`.\n' "$RED_RUN_ID" >> docs/development/ci.md
```

Expected: Run conclusion 为 failure，且失败原因唯一、可解释。

- [ ] **Step 4: 撤销受控失败并恢复 GREEN**

```bash
git rm scripts/ci/red_gate_probe.go
git add docs/development/ci.md
git commit -m "test: restore green CI after gate proof"
git push
```

Expected: 最终 diff 不包含 `red_gate_probe.go`。

- [ ] **Step 5: 记录 GREEN Run ID**

等待三个 Job 完成后运行：

```bash
GREEN_RUN_ID=$(gh run list --workflow ci.yml --branch feat/m0-003-ci-gates --limit 1 --json databaseId --jq '.[0].databaseId')
gh run view "$GREEN_RUN_ID" --json databaseId,conclusion,jobs
printf -- '- GREEN Run `%s`: `quality`, `generated-and-security`, and `windows-build` all passed after the fixture was removed.\n' "$GREEN_RUN_ID" >> docs/development/ci.md
git add docs/development/ci.md
git commit -m "docs: record M0-003 RED GREEN evidence"
git push
```

Expected: Run conclusion 为 success，三个 Job 均成功。

- [ ] **Step 6: 执行完成前全量验证**

```bash
go test ./scripts/ci -v
go run ./scripts/ci all
go test ./...
cd frontend
pnpm install --frozen-lockfile
pnpm test:run
pnpm typecheck
pnpm lint
pnpm build
cd ..
git diff --check
git status --short
```

Expected: 所有命令成功，工作区干净，没有故意失败文件、临时 Workflow、Secret、范围外业务代码或未提交生成漂移。

- [ ] **Step 7: 完成整分支代码审查**

审查范围：

1. `.github/workflows/ci.yml` 权限、Trigger、Runner、SHA Pinning 与失败语义。
2. `scripts/ci` 的正常和违规 Fixture 是否真正覆盖设计风险。
3. Gitleaks 是否使用 `fetch-depth: 0` 和脱敏输出。
4. Windows 烟测是否始终清理进程树。
5. 最终文件清单是否没有 SSH、数据库、AI、SQLite 业务表、凭据或 Release 配置。
6. 项目状态是否仍为 `IN_REVIEW`。

Critical 或 Important 问题必须修复并重新验证。

- [ ] **Step 8: 更新 PR 描述并转为 Ready for Review**

PR 描述必须列出：

- 三个 Job 的职责
- 固定 Action SHA 和工具版本
- RED Run ID 与失败步骤
- GREEN Run ID 与三个成功 Job
- Windows EXE 构建与烟测结果
- 最终无失败 Fixture、临时 Workflow 或范围外能力
- 当前 `M0-003=IN_REVIEW`

完成后：

```bash
gh pr ready
```

---

### Task 7: 合并、自举 Branch Protection 并完成项目状态闭环

**Files:**
- Modify after implementation merge: 7 个项目进度文件
- Create after implementation merge: M0-004 GitHub Issue

**Interfaces:**
- Consumes: 已批准且三个 Job GREEN 的实现 PR、实际 Merge Commit。
- Produces: `main` required checks、`M0-003=DONE`、`M0-004=READY`、关闭的 Issue #7。

- [ ] **Step 1: 合并前重新确认 Head 与评审状态**

确认：

- 无未解决评审线程
- 最新 Head SHA 与已验证 GREEN Run 对应
- 三个 Job 全部成功
- PR 可合并
- 最终 diff 无 `red_gate_probe.go`

使用 squash merge，Commit 标题固定为：

```text
ci: establish ItemAll quality and Windows gates
```

- [ ] **Step 2: 配置 main required status checks**

在 GitHub Repository Settings → Branches → `main` Branch Protection 中启用：

- Require status checks to pass before merging
- Require branches to be up to date before merging
- Required checks：`quality`、`generated-and-security`、`windows-build`
- 禁止 force push
- 禁止 branch deletion

不在本任务中强制两人 Review；仓库当前是单维护者模式。不要覆盖仓库已有的其他保护设置。

- [ ] **Step 3: 创建 M0-004 Issue 并设为 READY**

Issue 标题：

```text
M0-004: 建立 SQLite WAL / Migration / Repository 骨架
```

Issue 必须引用 M0-003 Merge Commit，并保留 Backlog 中的验收标准：首次启动迁移成功、重复运行幂等、没有业务功能扩张。

- [ ] **Step 4: 创建独立状态同步分支和 PR**

从实现 Merge Commit 创建分支：

```text
docs/m0-003-completion-sync
```

统一更新 7 个项目进度文件：

- `M0-003`: `IN_REVIEW → DONE`
- `M0-004`: `NOT_STARTED → READY`
- 当前总数：`DONE=3`、`READY=1`、`NOT_STARTED=46`
- `M0-foundation`: 完成 `3/10 = 30%`
- 全项目：完成 `3/50 = 6%`
- M0-003 完成证据：实现 PR、Merge Commit、RED Run、GREEN Run、三个 required checks
- M0-004 关联新 Issue
- Business Code：状态同步 PR 不修改业务代码

- [ ] **Step 5: 使用状态同步 PR 验证 Branch Protection**

状态同步 PR 必须自动出现并要求：

- `quality`
- `generated-and-security`
- `windows-build`

在三个检查全部成功前，GitHub 合并按钮必须被保护规则阻断。记录该事实到 PR 描述或周报。

- [ ] **Step 6: 合并状态同步 PR 并关闭 Issue #7**

状态同步 PR 仅包含 7 个项目管理文档，确认总数一致后 squash 合并。随后在 Issue #7 评论：

- 实现 PR 与 Merge Commit
- RED/GREEN Run ID
- required checks 生效证据
- 状态同步 PR 与 Merge Commit

以 `completed` 关闭 Issue #7。

- [ ] **Step 7: 最终验证 main 状态**

确认：

- `.github/workflows/ci.yml` 存在于 `main`
- 最新 `main` push 的三个 Job 全部成功
- Branch Protection 要求三个稳定检查
- Backlog 为 `DONE=3`、`READY=1`、`NOT_STARTED=46`
- M0-003 为 `DONE`
- M0-004 为 `READY`
- Issue #7 已关闭
- 没有遗留临时分支中的失败 Fixture进入 `main`

---

## Implementation Review Checklist

- [ ] Spec 1–17 节均有对应 Task。
- [ ] Workflow 只有一个文件和三个稳定 Job。
- [ ] 所有 Action SHA 与官方 Tag 一致。
- [ ] Gitleaks v8.30.0 扫描完整历史且输出脱敏。
- [ ] Ubuntu Wails 依赖与 `webkit2_41` 同时存在于两个 Ubuntu Job。
- [ ] pnpm 缓存只缓存 store，不缓存构建产物。
- [ ] `scripts/ci` 规则具备正常与违规 Fixture。
- [ ] Windows 烟测清理完整进程树。
- [ ] RED→GREEN 有实际 Run ID，最终 diff 不含失败 Fixture。
- [ ] 实现 PR 合并前状态保持 `IN_REVIEW`。
- [ ] required checks 在状态同步 PR 上实际阻断未通过的合并。
- [ ] 最终状态为 `DONE=3`、`READY=1`、`NOT_STARTED=46`。
